name: main
env:
  WORKING_DIR: $HOME/code/git/dotfiles
on: [push]
jobs:
  setup-environment:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v3
      - name: Install Ubuntu os package
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt update
          sudo apt install -y justfile
      - name: Install package on macOS
        if: matrix.os == 'macos-latest'
        run: |
          brew update
          brew install justfile
        with:
          python-version: '3.10'
      - run: |
          just setup
